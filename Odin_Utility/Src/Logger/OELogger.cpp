#include "OdinEngineLogger.h"

/*
namespace OE_SDK
{

	C_Logger::C_Logger()
	{

	}

	C_Logger::~C_Logger()
	{

	}

	void C_Logger::SetMessage(e_MessageType eType)
	{
		m_eMessage = eType;
	}

	void C_Logger::PrintMessage(char* prmMessage)
	{
		/*
		char* ptrMessage;
		char* ptrWarning;
		char* ptrError;
		char* ptrCriticalError;


		ptrMessage		= "Tienes un mensaje :3";
		ptrWarning		= "Tienes un warning eda'";
		ptrError		= "Tienes un error";
		ptrCriticalError= "Tienes un error crítico";
		*/
/*
		if (m_eMessage == MESSAGE)
		{
			m_File.WriteTextInFile(prmMessage);
		}

		if (m_eMessage == WARNING)
		{
			m_File.WriteTextInFile(prmMessage);
		}

		if (m_eMessage == EROR)
		{
			m_File.WriteTextInFile(prmMessage);
		}

		if (m_eMessage == CRITICAL_ERROR)
		{
			m_File.WriteTextInFile(prmMessage);
		}
		*/
	